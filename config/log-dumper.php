<?php

return [

    'timber' => [
        /*
         * When you use `ld` the logging info can be sent to Timber.
         */
        'send_ld_calls' => true,

        /*
         * Regular logging calls can be sent to timber as well.
         */
        'send_regular_log_calls' => true,

        /*
         * The port number to communicate with Timber
         */
        'port' => 3000,
    ],

];
